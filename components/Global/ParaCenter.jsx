import React from 'react';

const ParaCenter = ({text}) => {
    return (
        <p className='text-sm md:text-base font-normal   text-left text-center'>{text}</p>
    );
}

export default ParaCenter;
